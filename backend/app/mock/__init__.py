"""MOCK=1 support: the [ЖИШЭЭ] sample dataset lives in contracts/fixtures/processed and is
served by the in-memory graph (app/graph/memory.py). record.py regenerates the example
responses in contracts/fixtures/*.json from that dataset."""
